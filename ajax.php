<?php
	/*
	 
	 * 	用点进行字符串的连接	.
	 * 	分号和回车是解耦的，只有分号代表一行命令的结束
	 * 
	 * 
	 * 	php里的数组可以是索引数组和关联数组;
	 * 
	 * 
	 * 	xml是一种跨平台，跨语言的标签语言，通过标签来描述（保存）数据，html是xml的扩展。
	 * 
	 * 	语法规则，只能有一个根元素，
	 * 			  没有预定义的标签，
	 * 			 标签的定义不能以数字开头
	 * 
	 * 
	 * ajax的返回值类型
	 * 	ajax.responseType=
	 * 		json：json
	 * 		text：文本
	 * 		document:文档
	 * 		arraybuffer:
	 * 		bolb:二进制
	 * */
//	$aa=array(
//		"aa"=>"zhangsan",
//		"bb"=>"lisi"
//	);
//	$cc=$_GET["name"];
////	if($cc=="18"){
//		echo $cc;
//	}
//	else{
//		echo "error";
//	}
?>
<h1>h1</h1>